import { Course, Plan, PlanData } from '../models';
export declare const start: {
    date: Date;
    timezone: string;
};
export declare const planData: PlanData;
declare const course: Course;
declare const plan: Plan;
export { course, plan };
